

//Tuples are a special type in TypeScript that allows you to create an array with a fixed number of elements, where each element can have a different type.

let tuple: [string, number, boolean];
tuple = ["Alice", 30, true];
console.log(tuple)
